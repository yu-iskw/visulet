# Copilot instructions

Repository-wide guidance for humans and Copilot features is defined in **`AGENTS.md`** at the repository root. Prefer reading and updating that file so Cursor, Codex, Claude Code, Gemini CLI, and Copilot coding agent stay aligned.

Claude Code–only workflows (slash skills, `.claude/` hooks) are described in **`CLAUDE.md`**.
